# CORRECTIONS.md — history of where this repo and the spec diverged

`CLAUDE.md` says: "When this repo and the spec disagree, the spec wins — unless you believe the
spec is wrong, in which case stop and say so." This file is where "said so" got recorded during
Phases 1–3. Each finding has since been folded directly into `docs/BASE-DESIGN.md`,
`docs/APP-DESIGN.md`, or `docs/INTEGRATION-GUIDE.md` — those three are the single source of
truth from Phase 4 onward, not this file.

What's kept here is the **history**: a one-line pointer to where each correction landed and
what the original error was. Several of these are traps that will recur when the app-package
template gets built (the `banned-api`/factories subpath problem in particular), so the record
stays even though the docs no longer need it to be correct.

## Applied (Phases 1–4)

1. **Pre-commit mypy hook is `repo: local`, not a pinned mirror** — `docs/APP-DESIGN.md` §3.6, `docs/BASE-DESIGN.md` §5.1. Original spec text showed mypy as a pinned-`rev` mirror hook; a mirror's isolated venv can't satisfy `strict = true` + `mypy_django_plugin` + a `django_settings_module`, which needs the whole project importable.
2. **The `*.factories` rule is a grep hook, not a `banned-api` ruff entry** — `docs/BASE-DESIGN.md` §5.2. Spec asked for a `*.factories` entry in the `banned-api` table; `per-file-ignores` disables a rule *code* per path, and `core/**` must already ignore `TID251`, so a factories entry would be silently disabled exactly where it needs to catch violations — not expressible in ruff, enforced by grep instead.
3. **Security settings block (`SECURE_*`/cookie/HSTS)** — `docs/BASE-DESIGN.md` §4.3 (settings excerpt) and §7 (CI env). §4.3's shown code block omitted this block entirely, and without it `manage.py check --deploy --fail-level WARNING` (§7's CI job) fails against an otherwise-unset settings module.
4. **`jazzmin` precedes `django.contrib.admin` in `INSTALLED_APPS`** — `docs/BASE-DESIGN.md` §4.3. Spec's excerpt listed `django.contrib.admin` first; jazzmin overrides admin templates via the *first* matching app in `INSTALLED_APPS`, so the original ordering silently unthemes the admin with nothing to catch it.
5. **Request-ID `ContextVar`/middleware/filter live in `config/logging.py`** — `docs/BASE-DESIGN.md` §3. Spec required request-ID correlation but named no file for the plumbing and didn't list a `middleware.py` in §2's tree; co-located in `config/logging.py` instead, alongside `build_logging_config()`, since all three exist only to serve it.
6. **WebSocket composition point** — `docs/BASE-DESIGN.md` §3, "WebSockets" subsection. Not a wrong statement to correct, a gap: the spec never said how `config/asgi.py`'s `ProtocolTypeRouter` composition works, who owns it, or the `get_asgi_application()`-before-any-consumer-import ordering requirement.
7. **Realtime as an app package's fourth public surface** — `docs/APP-DESIGN.md` §6, "Realtime (optional fourth surface)" subsection. Same category as #6: the two-surface rule (`signals.py`/`services.py`) never addressed what a package exposes for a WebSocket consumer.
8. **The ephemeral test stack (`docker-compose.test.yml`) and a minimal `Makefile` pulled forward to Phase 3** — landed in the repo (`Makefile`, `docker-compose.test.yml`), not in one of the three design docs; their *content* (`docs/BASE-DESIGN.md` §5.3, §10.2) was already correct. `docs/CLAUDE-CODE-GUIDE-BASE.md`'s phase breakdown put these in Phases 5/8, leaving no phase before 8 with a tracked way to run the tests Phase 3 writes. **Not folded into the guide — it wasn't one of the three docs named for this pass; flagged as a decision, not silently applied.**
9. **`config/tests/` scoped into Phase 3, not left for later** — same shape as #8: a phase-sequencing fact, not a content error in `docs/BASE-DESIGN.md` §5.4 or `docs/INTEGRATION-GUIDE.md` §4 (both already correctly describe *what* to test). `backend/pyproject.toml`'s `--cov=config --cov-fail-under=80` makes this load-bearing regardless of which phase the guide assigns it to. **Same flag as #8.**
10. **The `tools/mixins.py` error envelope, previously unspecified** — `docs/BASE-DESIGN.md` §3 (envelope shape + `code` list) and `docs/APP-DESIGN.md` §4 (cross-reference). Spec described `tools/mixins.py` in one line ("shared DRF mixins/error formats") with no concrete shape for `APP-DESIGN.md` §4's "same shape" to point at.
11. **`docker-compose.test.yml`/`Makefile` `test` target gaps** — `docs/BASE-DESIGN.md` §5.3 and §10.2 (both code blocks). `test-redis` had no healthcheck, so `up -d --wait` didn't actually wait for it; the `test` target set only `POSTGRES_HOST`/`POSTGRES_PORT`, leaving the suite on `backend/.env`'s dev credentials and `redis://redis:6379/0` — neither reachable from the test containers.
12. **Bug, not a spec deviation: `RequestIDMiddleware` broke every real request** — `docs/BASE-DESIGN.md` §3 (logging bullet). A Phase 2 implementation bug, not a place the spec was wrong: the middleware never called `markcoroutinefunction(self)`, so `SecurityMiddleware` (wrapping it) couldn't detect it as async and called it without awaiting — confirmed to 500 every request through the real ASGI app, not just the test client. Found and fixed in Phase 3; regression-tested in `backend/config/tests/test_asgi_integration.py`.
15. **The SDK-to-host frontend client contract was unspecified, and the one code example touching it was self-contradictory** — `docs/APP-DESIGN.md` §12 (new "SDK-to-host client contract" and "When provider nesting gets unwieldy" subsections, updated manager/hook examples, updated §2 folder tree, updated §8 README usage example), `docs/INTEGRATION-GUIDE.md` §2 (new provider-mount step, install step 10; removal steps updated to match), §4 (composition example annotated), `docs/CLAUDE-CODE-GUIDE-APP.md` Phase 5 (file list and review criteria updated). §12 said an SDK's base URL "comes from the host's shared client configuration," but its own manager example did `import { apiClient } from "./client"` as though the SDK owned a client module — the SDK can't import the host's `frontend/lib/api-client.ts` (project-owned, not part of a publishable package) and the host isn't a publishable package either, so neither direction of that import can exist. Resolved as runtime dependency injection via React context: each SDK declares a structurally-typed `HttpClient` interface matching `frontend/lib/api-client.ts`'s actual `ApiClient` shape (`get`/`post`/`patch`/`delete`), ships a `<XProvider client basePath>` + internal `useXConfig()` in `api/context.tsx` (replacing the old `api/client.ts`), and the manager becomes instance-based — constructed per-hook via `useMemo` from the injected client, never a static class. `basePath` is a provider prop (defaulting to the app's suggested prefix) because `INTEGRATION-GUIDE.md` §3 already said the mount path is the host's choice, which the old hardcoded manager paths (`apiClient.get("/")`) never honored. Doc-only correction — no code changed, since no app package exists yet to hold the code this describes.

## Applied (Phase 5)

16. **`.gitignore`** (no BASE-DESIGN.md section — not a spec code block) — unanchored `lib/` silently excluded `frontend/lib/` from git; builds only worked because the files sat on disk.
17. **`docs/BASE-DESIGN.md` §8.1** (dev `backend/Dockerfile` block) — the block contradicted its own `.venv`-gotcha prose; never set `UV_PROJECT_ENVIRONMENT`, so the bind mount shadowed the container's venv.
18. **`docs/BASE-DESIGN.md` §8.1** (prod `backend/Dockerfile.prod` `CMD`) — hardcoded `--workers 3` against its own "derive from CPU count" notes bullet; uvicorn reads `$UVICORN_WORKERS` natively, so the flag is gone and compose sets the env var instead.
19. **`docs/BASE-DESIGN.md` §4.2 and §8.1** — `uv sync --no-dev` only excludes the `dev` group, not `test` (`[tool.uv] default-groups = ["dev","test"]`); the prod builder needed `--no-default-groups`, and would have shipped `pytest`/`factory-boy`/etc. into production otherwise.
20. **`docs/BASE-DESIGN.md` §8.2** (`celery-beat` command) — the healthcheck polled a pidfile the command never wrote; celery beat's own default is no pidfile at all. Added `--pidfile=/tmp/celerybeat.pid`.
21. **`docs/BASE-DESIGN.md` §8.2 and §3** — spec named `mailhog`, unmaintained since 2020; replaced with pinned `axllent/mailpit`.
22. **`docs/BASE-DESIGN.md` §3** (new email bullet) — `config/settings.py` had no `EMAIL_*` settings at all, so nothing pointed at mailhog or mailpit; added them plus `config.E004`.
23. **`docs/BASE-DESIGN.md` §8.1 and `CLAUDE.md`'s pinned-versions table** — the `uv` image pin (`0.9`) trailed the toolchain that writes `backend/uv.lock` (`0.11.19`) by two minors, and the table never listed `uv` or `Redis` at all.
24. **`docs/BASE-DESIGN.md` §8.2** (frontend healthcheck fragment) — `http://localhost:3000/...` is permanently unreachable on `node:22-alpine` (musl resolves `localhost` to `::1` first; BusyBox `wget` doesn't fall back to IPv4). Verified empirically. Changed to `127.0.0.1`.
25. **`docs/BASE-DESIGN.md` §8.2** (backend healthcheck) — needed a real decision, not a mechanical edit; see "Needed a decision" below. Final shape: prod healthcheck sends `-H "X-Forwarded-Proto: https"` rather than exempting `/healthz/` from `SECURE_SSL_REDIRECT` at the settings level; `ALLOWED_HOSTS` guidance now requires `127.0.0.1`.
26. **`docs/CLAUDE-CODE-GUIDE-BASE.md` Phase 9** — verification had no fresh-clone step; #16 above would have been caught immediately by one. Added a `git clone`-based check ahead of the final "follow README" step.
27. **`docs/CLAUDE-CODE-GUIDE-BASE.md` Phases 5 and 8** — the guide assigned the entire `Makefile` to Phase 8, but its compose-dependent targets are unblocked as soon as `docker-compose.yml` exists (Phase 5). Split accordingly; `lint`/`fmt`/`typecheck`/`check`/`deploy` remain Phase 8.
28. **`docs/BASE-DESIGN.md` §8.2 and §9, `backend/config/views.py`** — `/healthz/` (unauthenticated by design) returned the raw `str(exc)` from a failed DB/cache check, which names the internal hostname (confirmed empirically: `"failed to resolve host 'db': ..."`, `"...connecting to redis:6379..."`). Masked to a generic `"unavailable"` string when `DEBUG` is off, matching `tools/mixins.py`'s existing DEBUG-gated exception-detail pattern; real detail still logged server-side. Added a §9 note that nginx must not proxy `/healthz/` to the public internet regardless.

### Needed a decision, not a mechanical edit

Two Phase 5 findings weren't simple "match the prose" fixes — recorded here so they aren't mistaken for routine edits:

- **#25** — the initial fix (Phase 5 draft) exempted `/healthz/` from `SECURE_SSL_REDIRECT` via `SECURE_REDIRECT_EXEMPT`, which works but makes the endpoint reachable over plaintext wherever the path is proxied. Replaced with a healthcheck-side `X-Forwarded-Proto: https` header, which uses the trust path (`TRUST_PROXY_SSL_HEADER`) the app already has instead of adding a new exemption. No settings-level bypass remains; plain HTTP to `/healthz/` still redirects like every other path.
- **#28** — surfaced only because #25 prompted a closer look at what `/healthz/` actually returns on failure; not something the original defect list anticipated. The masking behavior (real detail in dev, generic in prod) is a design choice, not a bug fix with one obvious answer — flagged in case a future project wants the opposite trade-off (e.g., an internal-only healthcheck endpoint that's fine leaking detail).

## Applied (Phase 6)

29. **`docs/BASE-DESIGN.md` §7** (`backend-tests` env) — `SECRET_KEY: ci-only-not-a-secret` is 20 characters and trips `security.W009` (needs ≥50 chars, non-`django-insecure-`-prefixed); `check --deploy --fail-level WARNING` exited 1. Verified empirically against a `git archive` export with no local `.env` (the true CI condition). Replaced with a `Generate ephemeral CI keys` step that writes a `secrets.token_urlsafe(64)` value to `$GITHUB_ENV` at runtime, rather than a longer literal — see #31.
30. **`docs/BASE-DESIGN.md` §7** (`backend-tests` env) — the job set no `EMAIL_BACKEND`; `config/settings.py` defaults it to the SMTP backend with `EMAIL_HOST=mailpit`, which trips `config.E004` as an **ERROR** (not a warning) once `DEBUG=False`. Verified in the same run as #29 — fixed together, both blocked `check --deploy` on the first push. Set `EMAIL_BACKEND=django.core.mail.backends.locmem.EmailBackend`, which is also the correct backend for a test run regardless of the check.
31. **`docs/BASE-DESIGN.md` §7** (`backend-tests` env) — `FERNET_KEY: ${{ secrets.CI_FERNET_KEY }}` requires a GitHub secret configured per clone and is withheld from fork PRs, so a fork's CI never goes green. Replaced with the same runtime-generation step as #29 (`base64.urlsafe_b64encode(os.urandom(32))`, stdlib only, no `cryptography` import needed) — verified the generated value round-trips through `Fernet.encrypt`/`.decrypt`. This makes the workflow require zero configured GitHub secrets; stated as an explicit rule in the workflow's header comment, with the one expected future exception (a private-repo app-package token, `APP-DESIGN.md` §1.2) named alongside it.
32. **`docs/BASE-DESIGN.md` §7** — `PYTHON_VERSION: "3.14"` was declared at workflow level and never referenced anywhere in the file; `security-audit`'s `setup-node` hardcoded `node-version: "22"` instead of using the declared `NODE_VERSION`. Dropped `PYTHON_VERSION` (`.python-version` is the single source of truth and `setup-uv` already reads it — a second declaration is a second place to drift) and wired `security-audit` to `${{ env.NODE_VERSION }}`.
33. **`docs/BASE-DESIGN.md` §7** (`frontend` job) — ran `ruff format --check` on the backend but no formatting check on the frontend, so `prettier` was enforced only by pre-commit, which is opt-in per clone and therefore not a real CI gate. Added `npm run format:check` (script already existed in `frontend/package.json`; verified `npx prettier --check .` passes on the current tree).
34. **`docs/BASE-DESIGN.md` §7** (`security-audit`) and **`docs/APP-DESIGN.md`** (app-package CI template, same job) — both used `uv export --no-dev`, the identical defect CORRECTIONS #19 already found and fixed in the prod Docker builder: `--no-dev` is only an alias for `--no-group dev` and leaves the `test` group in the exported/audited set. Verified empirically: `--no-dev` exports 6 test-group packages (`pytest`, `factory-boy`, `freezegun`, …), `--no-default-groups` exports 0. Fixed both call sites to `--no-default-groups`, added `--locked` (fail loudly on a stale lock rather than silently audit a stale dependency set) and `--no-deps` on the `pip-audit` side (the export is already fully resolved with hashes, so re-resolving is redundant).
35. **`docs/BASE-DESIGN.md` §4.1** (prose, line 238) — "keep test/lint tooling out of production images with a single `--no-dev` flag" directly contradicted §4.2's own correction three sections later (`--no-dev` alone is insufficient; `--no-default-groups` is required). Same root cause as #19 and #34 — three live sites of one defect, found by `git grep -- '--no-dev'` across the whole repo rather than by reading §7 in isolation. Corrected the prose to name `--no-default-groups` with a forward reference to §4.2.
36. **`docs/BASE-DESIGN.md` §7** (`docker-build`) — no mention of `--ssh`, though `backend/Dockerfile.prod` mounts `--mount=type=ssh` on its `uv sync` layers for private app-package refs (`APP-DESIGN.md` §1.2). Not a failure today — BuildKit treats an unconnected `type=ssh` mount as optional by default — but nothing said what changes once a project installs its first private app package. Added a comment on the job naming the concrete addition (`webfactory/ssh-agent` step, `--ssh default` on the buildx call) and a matching note in `docs/INTEGRATION-GUIDE.md` §2 at the point someone actually hits it (installing the backend half of a private-repo app).

The three findings below (#37–#39) surfaced only on the real GitHub Actions runner opened for PR #1 (`ci/github-actions`) — none of them were reachable from a local `git archive` reproduction, because local reproductions all had a `backend/.env` or explicit env exports masking exactly the gap each bug depended on. This is the first-real-PR verification step `CLAUDE-CODE-GUIDE-BASE.md` Phase 6 calls for, and it earned its keep: 3 of 5 jobs failed on the first push despite every job passing local, isolated reproduction first.

37. **`docs/BASE-DESIGN.md` §7** (`backend-quality`) — the job set no environment at all, but `uv run mypy .` uses `django-stubs`' `NewSemanalDjangoPlugin`, which calls `django.setup()` to introspect models — importing `config/settings.py`, whose `decouple.config(...)` calls with no default (`SECRET_KEY`, `ALLOWED_HOSTS`, `FERNET_KEY`, `POSTGRES_DB`/`POSTGRES_USER`/`POSTGRES_PASSWORD`, `REDIS_URL`) raise `UndefinedValueError` with none of them set, which mypy surfaces as an opaque `INTERNAL ERROR -- Error constructing plugin instance of NewSemanalDjangoPlugin` rather than the real exception. Never caught locally: every local run of `mypy` up to this point had a `backend/.env` present, or explicit env exports left over from testing the deploy check, both of which supplied these values incidentally. Fixed by adding a job-level `env:` block of placeholder literals (`mypy` doesn't connect to Postgres/Redis, it only needs `config/settings.py` to import without raising) — verified with `env -i` (a genuinely empty environment) against a `git archive` checkout before pushing the fix.
38. **`docs/BASE-DESIGN.md` §7** (`backend-tests`, `uv run pytest -n auto` step) — 6 of 65 tests failed with `301` instead of the asserted status (`config/tests/test_healthz.py`, `test_wiring.py`, `test_asgi_integration.py`). Root cause: the job's `DEBUG: "False"` (needed for the deployment-check step) makes `SECURE_SSL_REDIRECT` default to `True` (`config/settings.py`'s `_SECURE_DEFAULT = not DEBUG`), and Django's test client makes plain-HTTP requests, so `SecurityMiddleware` redirects every one of them before the view under test ever runs. Never caught locally: `make test` (and every earlier local reproduction of this suite) always ran against `backend/.env`'s dev default `DEBUG=True`, which never triggers `_SECURE_DEFAULT`; the closest earlier check ran the deployment-check command in isolation, never the full `pytest` run under the same `DEBUG=False` env. The suite tests business logic (DB/cache down → 503, schema → 200), not TLS-redirect enforcement, so the fix is a step-level `env: { SECURE_SSL_REDIRECT: "False" }` scoped to only the `pytest` step — the deployment-check step above it keeps the strict default untouched. Verified against a real local Postgres + Redis (`docker-compose.test.yml`): reproduced all 6 failures with the job's exact env, then confirmed 65/65 pass with the override.
39. **`docs/BASE-DESIGN.md` §7** and **`docs/APP-DESIGN.md`** (`security-audit`) — `pip-audit --no-deps -r <(uv export ... --format requirements-txt)` failed with `THESE PACKAGES DO NOT MATCH THE HASHES FROM THE REQUIREMENTS FILE` for `psycopg-binary==3.3.4`'s `cp312-manylinux2014_x86_64` wheel. Investigated rather than papered over: fetched that exact file's `sha256` from PyPI's own JSON API (`e7510c3755...`) — it matches what pip actually downloaded and what CI reported as "Got", but matches **none** of the ~11 hashes `uv export` emitted for that package/version across platforms, meaning `backend/uv.lock`'s recorded hash for this specific wheel disagrees with the real, current, official PyPI artifact. `uv sync --locked` never surfaces this because uv verifies installs against its own lock, not pip's hash-checking mode — the mismatch is only reachable through `pip install`'s hash verification, which `uv export`'s hashed `requirements.txt` output triggers as a side effect. Fixed by adding `--no-hashes` to the export in both files: hash integrity of the resolved set is already `uv sync --locked`'s job elsewhere in the same workflow, and `security-audit`'s job is a CVE-database lookup by version, which doesn't need hash verification at all. `continue-on-error: true` meant this didn't block the PR, but its `gh api` job `conclusion` was still `"failure"` even with that flag set (confirmed: only the *workflow run's* conclusion, not the individual job's, is protected by job-level `continue-on-error`) — the reason `security-audit` must never be a required status check, only advisory.

### Needed a decision, not a mechanical edit (Phase 6)

- **#29/#31** — the alternative considered was widening `--fail-level` or hardcoding a longer literal key/secret directly in the workflow file. Rejected: a literal, however long, is still a value that sits in the repo and gets copied forward into every project cloned from this scaffold, eventually into a real `.env`. Runtime generation with the system `python3` (no dependency on the project's own venv existing yet) has no such failure mode and costs one extra step.
- **#39** — the alternative considered was chasing why `uv.lock`'s hash disagrees with PyPI (a stale lock, a rare legitimate PyPI-side re-upload, or a uv export bug) and re-locking to fix it at the source. Rejected for this phase: `uv lock --check` passes, meaning the lockfile is internally consistent and `uv sync --locked` installs correctly from it everywhere else in this workflow — the disagreement is specifically between `uv export`'s hash output and PyPI's live file, not between the lockfile and what gets installed. Scoping `security-audit` to stop relying on hash-checking (its job is CVE lookup, not integrity verification) fixes the actual failure without touching a lockfile that isn't wrong for its own purpose.

## Applied (Phase 7)

40. **`docs/BASE-DESIGN.md` §9 step 3** — listed required `.env.prod` files as root, `backend/`, **and `frontend/`**. Verified against `docker-compose.prod.yml`: only `./backend/.env.prod` is ever referenced via `env_file:` (4 services), and the root env arrives via `--env-file`. No service reads a `frontend/.env.prod`, and it cannot usefully exist — `NEXT_PUBLIC_API_URL` is a frontend build **ARG** (`docker-compose.prod.yml:161-166`), inlined into the client bundle at build time, sourced from the root env file. Dropped `frontend/` from the required-file check; §9 and §4.4 corrected.
41. **`docs/BASE-DESIGN.md` §4.4's env-file inventory omitted the root `.env.prod` entirely**, despite `docker-compose.prod.yml:10`'s `name: ${PROJECT_NAME:?set PROJECT_NAME in the root .env.prod}` hard-requiring it, and despite the root `.env.example`'s own header (lines 10-11) forward-referencing a root `.env.prod` that never existed. Added it to §4.4's table and added the missing `.env.prod.example` at the repo root — no `.gitignore` change needed, `!.env.prod.example` (line 78) is unanchored and already un-ignores it at any depth.
42. **`docs/BASE-DESIGN.md` §9:1092 asserted `deploy/deploy.prod.env` is gitignored; it was not.** Verified empirically with `git check-ignore -v` (no match). `.gitignore`'s `.env`/`.env.*` patterns both require a literal `.env` prefix on the path component; `deploy.prod.env` has none. This file holds `SERVER_HOST`/`SSH_KEY_PATH`, which is exactly what shouldn't leak into the tracked tree. Added a dedicated `deploy/deploy.prod.env` rule (the existing `!deploy/deploy.prod.env.example` negation was harmless but dead for the same reason — nothing was un-ignoring it from).
43. **`docs/BASE-DESIGN.md` §8.2/§9 implied building container identifiers from `container_name`/service names directly**, and §8.2's service-list line ("`db`, `redis`, `backend`, `frontend`, `celery`, `celery-beat`, and optionally `flower`") reads as if prod might conditionally include `flower`. Verified against `docker-compose.prod.yml`: it has **zero `profiles:` keys** — `flower`/`mailpit`/`debug-toolbar` are dev-only, gated by the `tooling` profile in `docker-compose.yml`, and don't exist in prod at all. Separately, service `celery-beat` names its container `${PROJECT_NAME}_celery_beat` (hyphen → underscore), so building names by substituting into a service-name pattern breaks on exactly that one service. `deploy-prod.sh` instead derives the expected set from `docker compose config --services` (verified locally to respect active profiles: dev with no profile returns 6 services, `--profile tooling` returns 8) and container IDs from `docker compose ps -q <service>` — correct today and if profiles are ever added to prod later.
44. **`docs/BASE-DESIGN.md` §9 step 6 (now step 7) required only `running`, not `healthy`.** Per Phase 7's brief, upgraded to check `docker inspect --format '{{.State.Health.Status}}'` — every service has a healthcheck as of Phase 5 (§8.2), so "running" is no longer sufficient; a container can be up and unhealthy.
45. **`docs/BASE-DESIGN.md` §9 step 7 ("Reload nginx if present") didn't say how** — nginx is not, and has never been, a compose service anywhere in this repo (`grep` for any nginx config/Dockerfile/service returns nothing); both `backend` and `frontend` publish only to `127.0.0.1` (§8.2), which only makes sense with a host-level reverse proxy in front. `deploy-prod.sh`'s nginx step therefore runs over SSH with `sudo` (`nginx -t`, then `systemctl reload nginx`), not through `docker compose exec`. §9 corrected to say so explicitly.

### Needed a decision, not a mechanical edit (Phase 7)

- **§9 step 1, "ideally, that CI is green on this commit."** Too vague to implement as written — "ideally" describes a preference, not a behavior. Decided: check by commit SHA (not branch, since the branch may have moved) via `gh run list --commit <sha>`, bounded with `timeout 30` so a network hang can't stall the deploy. Four distinct outcomes rather than one advisory bucket — `gh` missing/unauthenticated/no `origin` remote → **skip** with a note (the machine can't answer the question, that's not the commit's fault); no run found for the SHA → **fail** (the commit was probably never pushed, which is the same traceability hole the clean-tree check exists to close); still running → **fail** ("wait for it, or pass `--skip-ci-check`"); concluded anything but `success`/`skipped` → **fail**. `--skip-ci-check` bypasses the whole check. Rejected a single blanket "warn and continue": a check that never blocks doesn't stop a bad rollout, which is this script's one job per §9's closing paragraph. §9 amended to state the decision rather than "ideally".
- **`--backup-db` (or unconditional), per §9's own two options.** Decided: unconditional, with `--skip-backup-db` to opt out. An opt-in flag is exactly the thing that's forgotten under deadline pressure on the one deploy that needed it; an opt-out default means the safe behavior requires no one to remember anything.
- **Clean-tree override.** Considered adding an `--allow-dirty` escape hatch for hotfixes. Rejected — no override exists. This script rsyncs the working tree rather than a git ref, so the clean-tree check is the only thing that makes "what commit is production running" answerable after the fact (`DEPLOYED_VERSION`, written to the server right after rsync, is the payoff). An override wouldn't just risk untested code reaching prod, it would break that traceability for every deploy that used it. §9 states this reasoning explicitly rather than leaving it implicit.

## Applied (Phase 8)

46. **Closes former Open #14** — `INTEGRATION-GUIDE.md` §9 claimed `make check` ran vitest; §10.2's `check: lint typecheck test` target ran pytest only. Fixed by making `test` run both (`uv run pytest -n auto` then `npm run test -- --run`, both against the ephemeral Postgres/Redis stack, with teardown on success *or* failure — verified against a deliberate failure, not just the happy path).
47. **Closes former Open #15** — §10.2's `check` target, commented "Everything CI runs, locally," omitted `ruff format --check`, `npm run format:check`, `makemigrations --check --dry-run`, and `check --deploy --fail-level WARNING`, any of which could pass locally and fail in CI (or the reverse, since `make check` never set `DEBUG=False`). Fixed by folding the format checks into `lint` and adding a new `django-checks` target that runs the two Django management checks under a CI-matching env (`DEBUG=False`, `ALLOWED_HOSTS=localhost`, `SECURE_HSTS_SECONDS=31536000`, `EMAIL_BACKEND=…locmem…`, plus `SECRET_KEY`/`FERNET_KEY` generated at recipe time the same way CI does — CORRECTIONS #29/#31). `check` is now `lint typecheck django-checks test build`, and the Makefile states the governing rule directly: `check` is parity with `.github/workflows/ci.yml`, with a job-by-job map in a header comment, and two named exclusions (`docker-build`, `security-audit` — both a poor fit for a fast local gate, the latter already advisory-only per CORRECTIONS #39). Any new CI step must be mirrored in a target or documented as excluded there. Verified empirically: `manage.py makemigrations --check --dry-run` tolerates an unreachable DB (warns, doesn't fail), so `django-checks` needed no ephemeral-stack lifecycle of its own.

    *(Former Open #13 and #14/#15 shared a number with an already-assigned entry — Applied-Phases-1–4's #15 (the SDK-to-host client contract fix). #13 remains open below, unrenumbered; #14/#15 are retired by being closed here rather than reused.)*

48. **`docs/BASE-DESIGN.md` §10.2's `.PHONY` declared an `install` target with no body** — added: `uv sync --locked` + `npm ci` + `pre-commit install`, one step per line (a chained `cd a && x; cd b && y` on one line would let a failure in the first step go unnoticed once the second starts), plus a non-fatal reminder if `backend/.env` doesn't exist yet (created a step later, README step 4). §10.2's Makefile block updated to match.
49. **`docs/BASE-DESIGN.md` §11.1's file list was wrong in both directions** — it named `docker-compose*.yml`, which interpolates `${PROJECT_NAME}` at compose time and contains no literal placeholder, and omitted `backend/uv.lock`, `frontend/package-lock.json`, `backend/.env.example`, `.env.prod.example`, and `frontend/app/layout.tsx`'s `metadata.title`. The omission was live-breaking: renaming `pyproject.toml`/`package.json` without their lockfiles fails `uv sync --locked`/`npm ci` (README step 5) in every renamed clone. `scripts/rename-project.sh` now derives the file list by searching the tree for the placeholder instead of hardcoding it (via `find -prune` rather than `grep --exclude-dir`/`--exclude` — this machine's `grep` is aliased to `ugrep`, whose hidden-file and exclude-dir handling differ from GNU grep, and silently under- and over-matched during testing until switched to `find`), narrowly line-anchoring both lockfile edits to the root package's own `name` field, and explicitly excluding real `.env`/`.env.local`/`.env.prod`/`deploy.prod.env` files (gitignored, project-owned, never in scope) from the sweep. Verified end to end in a detached clone: `uv sync --locked` and `npm ci` both accept the renamed lockfiles with no fallback needed; a post-condition grep confirms no file is silently missed. §11.1 corrected to describe deriving the list rather than a fixed one.
50. **The Phase 8 brief (`CLAUDE-CODE-GUIDE-BASE.md`) assumed root `CLAUDE.md` was the host project's** — it's base-scaffold's own authoring instructions (working agreement, pinned-versions table), cited by §8.1 and still needed through Phase 9; `rename-project.sh` must never overwrite it. Resolved by moving `docs/CLAUDE.md.template` to the repo root (matching where `CLAUDE-CODE-GUIDE-BASE.md:45` and the template's own header already said it lived) and having `rename-project.sh` render only the host-project variant into a clone's `CLAUDE.md`, guarded on two conditions that must both hold: the file still carries the scaffold's marker heading (`# CLAUDE.md — base-scaffold (the template itself)` — a rendered or hand-edited `CLAUDE.md` is left alone), and the repo has zero commits (a truly detached clone, per README step 2, run *before* this script; a repo with commits is either this dev repo or an undetached clone — either way, left alone with an explanation printed). A fresh, undetached clone is byte-for-byte identical to this dev repo at this point, marker heading included — the commit check is what actually distinguishes them, not the heading alone. Verified both paths: a detached `/tmp` clone rendered correctly (zero `{{...}}` left except `{{ORG}}`/`{{APP_REGISTRY_URL}}`/the `{{e.g. ...}}` rows, which stay by design); a real run inside this dev repo left `CLAUDE.md` byte-identical and printed the skip reason. §10.1 and the Phase 8 brief corrected.
51. **`BASE-DESIGN.md` §10 step 4's Fernet-key line ran before step 5 installs `cryptography`** — `from cryptography.fernet import Fernet` on a bare `python3` fails on a fresh clone. Replaced with the stdlib-only equivalent already used in CI (`base64.urlsafe_b64encode(os.urandom(32))`, CORRECTIONS #31) — byte-for-byte the same key shape, works before `uv sync` has run. Step 5 changed to `uv sync --locked`, matching Docker/CI and proving `pyproject.toml`/`uv.lock` agreement on the very first install. README.md carries the corrected walkthrough verbatim.
52. **`BASE-DESIGN.md` §2's directory tree omitted `README.md`, `scripts/rename-project.sh`, and `CLAUDE.md.template`** despite §10 step 3 invoking the script and Phase 9 following the README — none of the three appeared in the tree that's supposed to be the map of the repo. Added.

## Open

13. **`CLAUDE-CODE-GUIDE-BASE.md` Phase 4's file list omits Prettier config, `format`/`format:check` scripts, and any frontend tests** — `docs/BASE-DESIGN.md` §7 (CI runs `npm run test -- --run`) and §10.2 (`make fmt` runs `npm run format`) both require them to exist; a vitest suite with zero test files exits non-zero, so §7's CI step would fail on the Phase 4 brief's file list alone. Scoped into Phase 4 rather than left for a later phase to discover broken.
